apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-cs582
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 128Mi
  storageClassName: standard
  volumeName: project2-pv-cs582
  selector:
    matchLabels:
      type: local